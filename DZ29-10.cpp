#include <iostream>
using namespace std;
/*int main()
{
    int x,y;
    cin >> x >> y;
    
    if ((y > 2*x-1) && (y < 2*x+1) && (1 )){
        cout <<"Точка лежит на заданой фигуре";
    }
    else{
        cout << "точка не лежит в задан";
    }

    return 0;
}
*/

/*int main()
{
int x,y;
cin >> x >> y;
if (( y > 2*x-2) && (y < 2*x-1) && (x > 2*x-2) && (x > 2*x-1) && (1)){
    cout <<"Точка лежит на заданой фигуре";
    
}
else{
    cout << "точка не лежит в задан";
    
}
 
}
*/
/*int main()
{
    int x,y;
    if ((x * x + y * y <=1) && (x>=0) && (y>= 0.5 * x -1 ) && (y >= 0.5 * x+1 )){
        cout <<"Точка лежит на заданой фишуре ";
    }
    else{
        cout << "точка не лежит в задан";
    }
    
}
  */
 
  
//
